# Frontend Testing Edge Cases
- Async tests must await user-visible outcomes.
- Brittle snapshots and test-id-only assertions can miss regressions.
- E2E tests should avoid real external services unless explicitly configured.