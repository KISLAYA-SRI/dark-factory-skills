# Frontend Testing Tool Contract
- Inspect package scripts, test config, Storybook config, Playwright/Cypress config, MSW handlers, and CI before adding tests.
- Prefer user-observable assertions over implementation details.
- Run targeted tests before broad suites.