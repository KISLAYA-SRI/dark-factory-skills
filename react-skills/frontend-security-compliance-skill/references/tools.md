# Frontend Security Tool Contract
- Inspect Next.js config, middleware, headers, package lockfiles, env files, dependency scan config, analytics, and consent code before editing.
- Run or report lint, tests, dependency audit/SCA, secrets scan, and header/CSP checks when configured.