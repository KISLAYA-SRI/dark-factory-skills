# Forms Edge Cases
- React Hook Form prefers uncontrolled registered fields; use Controller for controlled third-party components.
- Backend validation errors must map to fields or form-level messages.
- Multi-step persistence must not store sensitive data unsafely.

## System-Level Risk Patterns
- Prefer typed contracts, explicit boundaries, and realistic failure states.