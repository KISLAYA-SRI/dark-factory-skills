# Forms Compliance
- Fields must have accessible labels, descriptions, error messages, and focus behavior.
- Do not accept server-derived identity/audit/security fields from user input.

## Regulatory Context
Use supplied controls only. Apply WCAG, privacy, and security boundaries only when the skill scope requires them.