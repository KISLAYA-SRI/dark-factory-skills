# API Integration Compliance
- Do not expose server secrets in browser clients.
- Respect auth, tenant, rate-limit, idempotency, and error contracts from backend documentation.

## Regulatory Context
Use supplied controls only. Apply WCAG, privacy, and security boundaries only when the skill scope requires them.