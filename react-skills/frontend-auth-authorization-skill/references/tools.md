# Frontend Auth Tool Contract
- Inspect existing auth provider config, middleware, session helpers, route groups, API clients, and test setup before editing.
- Prefer server-side session checks for protected data and route access.
- Run or report auth route, denied-path, and session tests when configured.