# State Compliance
- Do not persist sensitive data in localStorage/sessionStorage unless explicitly approved.
- Keep URL state free of secrets and sensitive PII.

## Regulatory Context
Use supplied controls only. Apply WCAG, privacy, and security boundaries only when the skill scope requires them.