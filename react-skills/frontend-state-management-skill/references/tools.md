# State Management Tool Contract
- Inspect existing store/query/provider patterns before adding state.
- Prefer narrow local state before global stores.
- Use stable query keys and explicit invalidation.